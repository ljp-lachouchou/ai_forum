# 项目面试问答

本文档整理了在面试过程中可能会被问到的问题，帮助你更好地理解和介绍 AIForum 项目。

---

## 一、架构设计类问题

### 1. 为什么选择 Clean Architecture？它给项目带来了什么好处？

**回答要点：**
- Clean Architecture 将业务逻辑与外部依赖解耦，提高代码的可测试性
- 通过分层设计（展示层、领域层、数据层），各层职责清晰，便于维护
- 支持依赖反转，可以灵活替换数据源（如从 Supabase 切换到其他后端）
- 团队成员可以并行开发不同层，提高开发效率

### 2. 你是如何实现三层架构的？每层分别负责什么？

**回答要点：**
- **展示层**：Compose UI + ViewModel，负责渲染界面和处理用户交互
- **领域层**：Use Cases + Entities，封装核心业务逻辑，不依赖任何框架
- **数据层**：Repository + Room + Network，负责数据获取、缓存和持久化
- 各层之间通过接口通信，实现了依赖反转

### 3. 项目中如何处理数据流转？用户点击点赞后数据如何更新？

**回答要点：**
- 用户操作 → UI 触发 → ViewModel 调用 Use Case
- Use Case 调用 Repository，Repository 先更新本地数据库
- 同时发起网络请求同步到服务器
- 使用 Flow 或 StateFlow 观察数据变化，UI 自动刷新
- 采用了乐观更新策略，提升用户体验

### 4. 为什么采用模块化设计？这样做的优缺点是什么？

**回答要点：**
- **优点**：功能隔离、并行开发、按需加载、减少编译时间
- **缺点**：增加了项目复杂度，需要合理规划模块依赖
- 我们将 Core 模块和 Feature 模块分离，Feature 模块分为 API 和 Impl
- 这种设计支持团队协作和功能扩展

---

## 二、技术选型类问题

### 5. 为什么选择 Jetpack Compose 而不是传统 XML 布局？

**回答要点：**
- Compose 使用声明式 UI，代码更简洁直观
- 支持 Kotlin 编写，与项目语言一致
- 内置预览功能，开发效率更高
- 性能更好，避免了视图树的性能开销
- Google 官方推荐，是未来的发展方向

### 6. 为什么选择 Ktor 而不是 Retrofit？

**回答要点：**
- Ktor 是 JetBrains 出品，与 Kotlin 生态深度集成
- 协程支持更原生，代码更简洁
- 插件化设计，扩展性强
- 支持 WebSocket，满足实时通信需求
- 包体积更小

### 7. 为什么选择 Room 而不是直接使用 SQLite？

**回答要点：**
- Room 提供编译时 SQL 检查，避免运行时错误
- 支持 LiveData、Flow 集成，数据观察更方便
- 类型安全，减少了代码错误
- 内置数据库迁移方案，版本升级更简单
- Google 官方推荐的最佳实践

### 8. 为什么选择 Hilt 而不是手动依赖注入？

**回答要点：**
- Hilt 是 Google 官方的 Android DI 库，基于 Dagger 构建
- 编译时生成代码，运行时无性能开销
- 自动管理组件生命周期，减少样板代码
- 支持 ViewModel、WorkManager 等 Android 组件
- 社区活跃，文档完善

### 9. 为什么选择 WorkManager 进行后台任务调度？

**回答要点：**
- WorkManager 是 Google 官方推荐的后台任务解决方案
- 兼容不同 Android 版本，统一 API（Doze 模式、应用待机等）
- 支持约束条件（网络可用、电量充足、充电中、设备空闲等）
- 支持任务链和周期性任务
- 数据持久化，应用重启后仍可恢复任务
- 与 Hilt 集成，支持依赖注入

---

## 三、离线优先与数据同步类问题

### 10. 什么是离线优先策略？你的项目是如何实现的？

**回答要点：**
- **离线优先定义**：优先从本地数据库读取数据，确保应用始终可用
- **实现方式**：
  - Repository 优先查询本地 Room 数据库
  - WorkManager 在后台自动同步远程数据
  - Paging 3 RemoteMediator 协调本地和远程数据源
- **优势**：
  - 网络不稳定时用户仍可正常使用
  - 即时响应，提升用户体验
  - 减少网络请求，节省流量
  - 支持离线编辑，网络恢复后自动同步

### 11. WorkManager 在项目中的具体应用场景是什么？

**回答要点：**
- **数据同步**：定期从服务器拉取最新数据到本地数据库
- **离线操作上传**：用户离线时操作暂存本地，网络恢复后 WorkManager 自动上传
- **推送数据处理**：收到推送后，使用 WorkManager 在后台拉取详细数据
- **约束条件**：仅在 WiFi 或有网络时执行，节省用户流量

### 12. Paging 3 的 RemoteMediator 是如何工作的？

**回答要点：**
- **职责**：协调本地数据库和远程数据源
- **工作流程**：
  1. Paging 请求新数据时，先从数据库查询
  2. 如果本地数据不足，触发 RemoteMediator
  3. RemoteMediator 从服务器获取数据
  4. 将数据保存到本地数据库
  5. Paging 从数据库返回数据给 UI
- **优势**：实现离线优先，网络不可用时仍可浏览已有数据

### 13. 如何处理离线编辑和数据冲突？

**回答要点：**
- **离线编辑**：用户操作先保存到本地数据库，标记为待同步
- **同步策略**：WorkManager 检测网络可用后，自动上传待同步数据
- **冲突处理**：
  - 采用时间戳优先策略，最新数据覆盖旧数据
  - 对于敏感操作（如删除），服务器时间戳优先
  - 冲突数据记录日志，供后续分析
- **用户反馈**：同步成功/失败通过通知或提示告知用户

---

## 四、性能优化类问题

### 14. 项目中有哪些性能优化措施？

**回答要点：**
- 使用 Paging 3 实现列表分页加载
- Coil 图片加载库支持缓存和内存优化
- 离线缓存策略，减少网络请求
- LazyColumn 实现虚拟滚动，只渲染可见项
- StateFlow 避免不必要的重组

### 15. 如何处理大量数据的列表展示？

**回答要点：**
- 使用 Paging 3 分页库，按需加载数据
- RemoteMediator 结合网络和本地数据源
- LazyColumn 虚拟滚动，只渲染可见项
- 数据库索引优化查询性能
- 图片使用 Coil 加载，支持缓存和占位图

### 16. 如何优化应用启动速度？

**回答要点：**
- 模块化设计支持按需加载
- Application 初始化优化，使用延迟初始化
- ContentProvider 去除不必要的初始化
- 启动页优化，减少首屏渲染时间
- Baseline Profile 配置提升运行性能

---

## 五、问题解决类问题

### 17. 遇到过什么技术难题？你是如何解决的？

**回答要点：**
- **软删除问题**：点赞和收藏数据使用了软删除，需要正确处理软删除状态
- **心情主题切换**：动态主题切换需要处理状态恢复和动画效果
- **大图加载 OOM**：使用 Coil 的图片采样和缓存策略
- **离线优先同步**：实现 WorkManager + RemoteMediator 协调本地与远程数据

### 18. 如何处理网络异常和错误？

**回答要点：**
- 使用 Result 包装类统一封装返回结果
- Repository 层处理网络异常，转换为业务错误码
- ViewModel 层通过 StateFlow 暴露加载状态
- UI 层根据状态展示加载中、错误、空状态
- WorkManager 支持离线重试，网络恢复后自动重试失败的操作

### 19. 如何保证数据一致性？

**回答要点：**
- Repository 作为单一数据源入口
- **离线优先策略**：本地数据库作为主数据源，WorkManager 后台同步
- 使用 Room 的事务保证数据操作原子性
- Flow 观察数据变化，自动同步 UI
- 冲突解决策略：服务器时间戳优先，最新数据覆盖

---

## 六、开发流程类问题

### 20. 你们是如何进行测试的？

**回答要点：**
- **单元测试**：JUnit + Mockito，测试业务逻辑
- **UI 测试**：Roborazzi 截图测试，确保 UI 正确性
- **集成测试**：Robolectric 模拟 Android 环境
- **测试覆盖率**：核心业务逻辑覆盖率 > 80%

### 21. 代码规范是如何管理的？

**回答要点：**
- 使用 ktlint 进行代码格式化
- Detekt 进行代码静态检查
- Git pre-commit hook 自动检查
- Code Review 流程
- CI/CD 自动化测试

### 22. 团队协作是如何开展的？

**回答要点：**
- 模块化设计，按功能分工
- API 接口先行，并行开发
- Git Flow 分支管理策略
- 定期代码 Review
- 文档和注释完善

---

## 七、扩展与优化类问题

### 23. 如果需要支持多语言，你会如何设计？

**回答要点：**
- 使用 Android 原生资源文件管理多语言
- Compose 的 stringResource 函数获取字符串
- 服务器返回的数据也支持多语言字段
- 用户设置保存在 DataStore
- 应用启动时根据系统语言或用户选择加载对应资源

### 24. 如何添加深色模式支持？

**回答要点：**
- 使用 Material 3 的动态配色系统
- Compose 的 MaterialTheme 支持 isLight 参数
- 为亮色和暗色定义不同的 ColorScheme
- 监听系统主题变化，自动切换
- 保存用户偏好到 DataStore

### 25. 如何优化应用体积？

**回答要点：**
- 启用 R8/ProGuard 代码混淆和优化
- 移除未使用的资源（shrinkResources）
- 模块化按需加载
- 使用 WebP 格式替换 PNG 图片
- 动态功能模块（Dynamic Feature Modules）
- 分析 APK 大小，针对性优化

---

## 八、项目亮点类问题

### 26. 这个项目最大的亮点是什么？

**回答要点：**
- 完整的 Clean Architecture 实现，架构清晰
- 全面的模块化设计，支持团队协作
- 丰富的功能（帖子、评论、树洞、搜索、心情主题）
- 良好的代码质量和测试覆盖
- **离线优先策略**：WorkManager + RemoteMediator，确保网络不稳定时仍可用
- 完善的推送通知和数据同步机制

### 27. 从这个项目中学到了什么？

**回答要点：**
- 深入理解 Clean Architecture 的实际应用
- 掌握 Jetpack Compose 的高级用法
- 离线优先策略的设计与实现
- WorkManager 后台任务调度的实践
- 模块化设计对大型项目的重要性
- 性能优化的实践经验
- 团队协作和代码规范的建立

---

## 九、其他问题

### 28. 如果让你重新设计这个项目，你会怎么做？

**回答要点：**
- 更早地引入自动化测试
- 使用 Multiplatform 可能更合适（如果有跨平台需求）
- 更细粒度的模块划分
- 更完善的文档和 API 规范
- 引入更多性能监控工具

### 29. 你对自己在项目中的贡献最满意的是什么？

**回答要点：**
- 根据实际情况回答，比如：
  - 实现了心情主题切换功能
  - 解决了软删除带来的数据一致性问题
  - 优化了图片加载策略，避免 OOM
  - 完成了模块化架构重构

---

## 面试准备建议

1. **深入理解架构**：能够画出架构图，解释各层职责
2. **准备具体案例**：用实际代码和场景说明问题
3. **展示思考过程**：不仅是答案，还要说明为什么
4. **诚实面对不足**：承认问题，说明改进方案
5. **保持自信**：展示自己的能力和学习意愿
